<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.BookMapper">

    <!--增加一个Book-->
    <insert id="addBook" parameterType="Books">
        insert into spring.books(name,counts,detail)
        values (#{name}, #{counts}, #{detail})
    </insert>

    <!--根据id删除一个Book-->
    <delete id="deleteBookById" parameterType="int">
        delete from spring.books where id=#{id}
    </delete>

    <!--更新Book-->
    <update id="updateBook" parameterType="Books">
        update spring.books
        set name = #{name},counts = #{counts},detail = #{detail}
        where id = #{id}
    </update>

    <!--根据id查询,返回一个Book-->
    <select id="queryBookById" resultType="Books">
        select * from spring.books
        where id = #{id}
    </select>

    <!--查询全部Book-->
    <select id="queryAllBook" resultType="Books">
        SELECT * from spring.books
    </select>

    <!--按名称查询Book-->
    <select id="queryBookByName" resultType="Books">
        SELECT * from spring.books
        where name like "%"#{name}"%"
    </select>

</mapper>
